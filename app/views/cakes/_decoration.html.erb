<%= simple_form_for :cake, url: cakes_path(@cake_params), method: :post do |f| %>
<div class="row dough-form" style = "max-height: 100%;">
  <div class="col-md-8" style = "max-height: 100%;">
    <h1 id = "step-title" style = "font-size: 30px;">Escolha a decoração do bolo</h1>
    <div id = "decoration-wrapper" style = "max-height: 600px; overflow: scroll; padding-top: 20px;">
      <% @decorations.each do |decoration| %>

      <div class="col-md-4 card">
        <div class = "img-decoration-wrapper" >
          <div class="row img-decoration-inner-wrapper">
            <%= image_tag("cake_5.jpg") %>
            <div class="row">
            <div class="col-md-3">
                <div class="radio option-input i-filling" id = "i_<%= decoration.id %>"></div>
              </div>
              <div class="col-md-9" style = "display: flex; align-items: center;">
                <%= decoration.name %>
              </div>
            </div>
            <p >+<%= decoration.price_cents %>€/Kg</p>
            <p>A partir de 2kg</p>
          </div>

        </div>
        <div class="">
          <%= f.radio_button :decoration, decoration.id %>
        </div>
      </div>

      <% end %>
    </div>
  </div>
  <div class="col-md-4">

  <h1 id = "step-title" style = "font-size: 30px;">Tamanho</h1>
  <%= f.input :size, collection: Cake::SIZES %>

  <h1 id = "step-title" style = "font-size: 30px;">Mensagem</h1>
   <%= f.input :message, as: :text, input_html: {placeholder: "Texto para bolo"} %>
 </div>
</div>
<%= f.input :dough, as: :hidden, input_html: {value: @dough.id}%>
<%= f.input :filling, as: :hidden, input_html: {value: @filling.id}%>
<%= f.submit "Proximo passo", class: "btn btn-lg btn-default", id: "step2"%>
<% end %>


