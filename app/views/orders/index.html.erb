<% @orders.each do |order| %>
  <p>Order <%= order.id %></p>
    <% order.cakes.each do |cake| %>
      <p>
        <%= cake.size %> <%= cake.shape %> <%= cake.dough.name %> with <%= cake.filling.name %>
      </p>
    <% end %>
<% end %>
